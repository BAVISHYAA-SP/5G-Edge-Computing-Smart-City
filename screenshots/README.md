# Screenshots

Add screenshots here, for example:

- `netanim.png` — NetAnim animation window showing the topology / packet flow
- `terminal_output.png` — terminal showing a successful `./ns3 run scratch/mec_arvr_final`
- `python_graphs.png` — Python-generated graphs from `analysis.py`
- `matlab_results.png` — MATLAB-generated graphs from `mec_analysis.m`

(The actual result graphs already live in `../results/` as PNGs and are embedded
directly in the main README — these screenshots are for *process* evidence, e.g.
the live NetAnim window or a terminal session, which can't be generated
automatically.)
